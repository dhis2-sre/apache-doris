# Doris versions to publish, and the tiers each is published for. Please keep the versions sorted in
# descending order. The upstream tags are <tier>-<version>, for example apache/doris:fe-4.0.8.
#
# The policy is the latest patch of each line upstream still patches, which is currently 4.0 and 4.1.
# Older lines are deliberately absent: 3.1 last moved in January, 3.0 last October and 2.1 a year
# ago, and every entry here is two images rebuilt nightly. Add a line back when something actually
# runs it, since dropping one only stops the rebuild, it does not unpublish the tag.
jdbcVersion: 42.7.5
jdbcSha256: 69020b3bd20984543e817393f2e6c01a890ef2e37a77dd11d6d8508181d079ab
tiers:
  - fe
  - be
versions:
  - 4.1.3
  - 4.0.8
